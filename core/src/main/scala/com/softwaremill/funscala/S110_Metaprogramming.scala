package com.softwaremill.funscala

object S110_Metaprogramming {
  // Programs, which create programs
  
  /*
  Programs become data
  
  For example: a compiler, assembler, interpreter, linker
  
  Introspection: program that looks and reports on itself
  Reflection: program that modifies itself
   */
  
  // We will focus on programming the compiler
}
