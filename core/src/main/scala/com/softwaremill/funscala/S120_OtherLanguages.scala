package com.softwaremill.funscala

object S120_OtherLanguages:
  def macroSystems(): Unit = {
    /*
     * Lisp
     * Racket
     * Template Haskell
     * Clojure
     * Scala
     * Rust
     */
  }
  
  def templateMetaprogramming(): Unit = {
    // C++
  }
  
  def cPreprocessor(): Unit = {
    /*
    #define PI 3.14159 
    #define RADTODEG(x) ((x) * 57.29578)
    
    #ifdef __unix__
    # include <unistd.h>
    #elif defined _WIN32 
    # include <windows.h>
    #endif
     */
  }
  
  def codeGenerators(): Unit = {
    // probably every language
    // In Scala: Twirl, ScalaPB, sbt-boilerplate
  }
